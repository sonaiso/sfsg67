# Updated line
count = int(ctx.get("ambiguity_count", 0))  # type: ignore[arg-type]

#... other content remains unchanged up to the next line

# Updated line
count = int(context.get("ambiguity_count", 0))  # type: ignore[arg-type]
